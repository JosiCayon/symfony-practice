<header>
    <nav>
        <ul>
            <li><a href="{{ path('default_redirect_to_home') }}">Página de inicio</a>
            <li><a href="{{ path('default_index') }}">Empleados HTML</a> {# path() monta la ruta solo con el nombre de la Route #}
            <li><a href="{{ url('default_index_json', {_format: 'json'}) }}">Empleados JSON</a> {# url() monta la ruta completa. Los parámetros se pasan como un objeto tanto como path y url #}
        </ul>
    </nav>
</header>